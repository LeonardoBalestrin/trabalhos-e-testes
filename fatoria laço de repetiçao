#include <stdio.h>

int main()
{
    int f=1, n;
    printf("digite um numero:");
    scanf("%d", &n);
        while(n>0){
        f = f*n;
        n--;}

    printf("numero fatorado: %d", f);
    return 0;
}
